---
import { getCollection, render } from "astro:content";
import ArticleLayout from '../domains/blog/layouts/ArticleLayout.astro';

const abouts = await getCollection('about');
const about = abouts[0];
const { Content } = await render(about);
---

<ArticleLayout {...about.data}>
	<Content />
</ArticleLayout>
